#include <iostream>
using namespace std;

int main(){

    int a = 10;
    int b = 3;
    cout<<a%b<<endl;

    // if a > b or a == b then a%b remainder is

    // PROPERTIES
    // 1) if a < b then a%b = a
    // 2) if a % b = a % (-b)
    // 3) (-a) % b = -(a % b)
    // 3) (-a) % (-b) = -(a % b)

    

    return 0;
}